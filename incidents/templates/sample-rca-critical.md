# ✗ Incident: Database saturation causing API latency degradation

**Severity:** CRITICAL  
**Environment:** cxm-client-alpha  
**Generated:** 2026-05-12 14:31:07 UTC  
**Type:** `db_saturation_api_cascade`  

---

## Evidence

- ✗ **POSTGRES** — connection_pool_pct: `87.0%`
- ⚠ **HTTP** — api_latency_ms: `3210ms`
- ⚠ **POSTGRES** — long_running_queries: `3 queries`

### PostgreSQL Detail

| Metric | Value |
|--------|-------|
| Connection pool | 87.0% (87/100) |
| Long-running queries | 3 |
| Blocked queries | 0 |
| Cache hit ratio | 98.2% |
| Database size | 4321.4 MB |

**Longest queries:**

- PID 4821 — 142s — `SELECT count(*) FROM visit WHERE DATE(arrival_time) = CURRENT_DATE GROUP BY branch_i...`
- PID 4839 — 67s — `UPDATE scheduled_job SET status='RUNNING', started_at=NOW() WHERE id=...`
- PID 4851 — 34s — `SELECT * FROM statistics_daily WHERE branch_id IN (SELECT id FROM branch WHERE...`

---

## Likely Cause

Database connection pool saturation is likely causing query queuing, which cascades into elevated API response times. A long-running or blocking query may be holding connections.

---

## Recommended Actions

1. Review pg_stat_activity for blocking queries
2. Check application connection pool configuration
3. Identify any long-running reporting or stats jobs
4. Verify Qmatic background schedulers are not looping

---

*Generated by observability-control-plane / rca_generator.py*
