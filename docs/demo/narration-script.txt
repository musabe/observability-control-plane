This is Vorsa.
An operational intelligence platform built specifically for hosted Qmatic Orchestra environments.
You're looking at the Northvale Council environment.
A live Qmatic deployment serving citizen services.
Health score: 97.
PostgreSQL connection pool: 2.8 percent.
HTTP responding at: 228 milliseconds.
Three Qmatic services running.
45 JDBC connections across five databases.
Everything nominal.
...
The service topology reflects the real Qmatic Orchestra architecture.
Client requests — web, mobile, and kiosk — flow through the API Gateway.
The gateway routes to three core engines: Orchestra Core for queue and workflow management, the Appointment Engine for booking and calendar, and the Messaging Engine for notifications.
All three engines connect to the Operational Database — qp_central and qp_agent — and the Statistics Database — statdb.
The statistics database feeds the BI and historical reporting layer via ETL.
Every node is green.
...
Now I'm going to simulate a PostgreSQL service failure.
The most common and most impactful incident in a Qmatic deployment.
...
The platform has detected a complete infrastructure failure.
Health score has dropped from 97 to 12.
Critical.
PostgreSQL: offline.
HTTP: timeout.
Two Qmatic services stopped.
All five databases showing zero JDBC connections simultaneously.
That pattern — all databases dropping to zero at the same moment — is the signature of a database-layer failure.
Not an application issue.
...
The correlation engine has evaluated all nine rules.
Two patterns matched.
First:
Database unavailable.
PostgreSQL is not responding.
Confidence: 91 percent.
Five independent critical signals correlated in a single 60-second poll cycle.
Second:
Qmatic service stopped.
The engines cascaded as a result of losing the database layer.
Confidence: 88 percent.
Two rules.
Two named incidents.
One root cause.
...
Each incident includes a generated RCA artifact.
Evidence surfaced directly in the dashboard.
Specific remediation steps provided automatically.
Verify PostgreSQL is running.
Check network connectivity.
Review logs for crash or out-of-memory events.
Configure Windows Service Recovery for automatic restart.
The on-call engineer does not need to diagnose the issue from scratch.
The platform has already completed the correlation work.
...
The service topology shows exactly where the failure propagated.
API Gateway: stopped.
All three engines: stopped or degraded.
Both databases: zero JDBC connections.
The dependency chain is visible.
...
PostgreSQL service has now been restarted.
The platform tracks recovery automatically.
Health score climbing.
JDBC connections re-establishing across all five databases.
Services returning online.
Back to 100.
Healthy.
Zero incidents.
The full incident lifecycle — detection, correlation, RCA generation, and recovery — tracked automatically within a single 60-second poll cycle.
...
Vorsa is not a generic monitoring dashboard.
It understands Qmatic.
The database schema.
The service dependencies.
The JDBC connection patterns.
And the operational business context.
It detects infrastructure failures.
Application-layer degradation.
And data quality anomalies that generic observability tools cannot surface.